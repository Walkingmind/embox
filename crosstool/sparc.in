#sparc
#leon platform was added in 4.6 (sparc-leon-) and so on it means the same if use --with-tune=leon
#also --with-cpu=leon was added  to libgloss (binutils)

TARGET=sparc-elf
#TARGET_OPTIONS="--with-tune=leon --with-cpu=leon"
TARGET_OPTIONS="--with-tune=leon --with-cpu=v8"

#TARGET=sparc-leon-elf
#TARGET_OPTIONS=--with-soft-fp

PATCHES="tc-sparc.patch gengtype.patch"
