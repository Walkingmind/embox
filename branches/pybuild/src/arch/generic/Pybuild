
package('embox.arch.generic')

module( 
	'interrupt_stub',
	implements = ['embox.arch.interrupt'],
	sources = ['interrupt_stub.h'],
)

module(
	'syscall_stub', 
	implements = ['embox.arch.syscall'],
	sources = ['syscall_stub.h'],
)

module(
	'nommu',
	implements = ['embox.arch.mmu'],
	sources = ['nommu.h'],
)

module(
	'nousermode', 
	implements = ['embox.arch.usermode'],
	sources = ['nousermode.h'],
)
