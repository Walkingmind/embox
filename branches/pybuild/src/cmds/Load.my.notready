
package('embox.cmd')
raise Exception('AnnotatedType not supported, do it manually')

module(
	'load',

  	options = [
		,default=../linux/build/bin/String('image',
 		,default=../linux/build/bin/piggy),
	],

 	sources = [
		Rule(prerequisites=../linux/build/bin/piggy,'load_incbin.S'))),
 		'load.c',
 		'load.lds.S',
	],

 ) 