
package('embox.framework')

library(
    'LibFrameworkNet',
    depends = ['net.pack', 'net.proto', 'net.sock']
)

library(
    'LibFramework',
    depends = ['cmd', 'example', 'mod', 'service', 'test', 'runlevel']
)

module (
    'runlevel',
    sources = [
        DefMacro(['__FRAMEWORK__'],
            ["runlevel.c", "unit.c"])
        ],

    depends = [
        'mod', 
        'embox.prom.printf',
        'embox.lib.LibC',
    ],
)

