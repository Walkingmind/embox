
package('embox.mem')

interface('objalloc')

module (
    'pool_adapter',
    implements = ['objalloc'],

    depends = ['embox.mem.pool'],
    
    sources = [
        IncludePath(['src/compat/posix/include'],
            ["pool_adapter.c", "pool_adapter.h"])]
)

module (
    'slab_adapter',
    implements = ['objalloc'],

    depends = ['embox.mem.slab'],
    
    sources = [
        IncludePath(['src/compat/posix/include'],
            ["slab_adapter.c", "slab_adapter.h"])]
)
