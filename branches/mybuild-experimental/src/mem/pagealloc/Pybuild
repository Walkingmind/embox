
package('embox.mem')

#abstract module page_api { 
	#option number page_size=4096
#}

interface('page_api')

module (
    'base_page',

    options = [
        Integer('page_size', default = 4096)
    ]
)

module (
    'bitmask',
    
    super = 'base_page',

    implements = ['page_api'],

    sources = ["bitmask.c"],

    #depends = ['heap_lds'],
)
