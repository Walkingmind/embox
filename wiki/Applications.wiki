#summary Applications description

Applications are programs user directly interacts. In contrast with Services, which build for
another programs, application are build for user.

There are only two ways for application to get started:
 * Application is the only applicaiton is system
 * Application directly called by Shell.
