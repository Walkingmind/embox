
package ('embox.driver')

interface('ambapp_api')

module (
    'ambapp_dumb',
    implements = ['ambapp_api'],

    sources = ["ambapp_dumb.h"],
)

module (
    'ambapp',
    implements = ['ambapp_api'],

    sources = ["ambapp.h"],
)

module (
    'amba_registry',

    sources = [
        IncludePath(
            ['src/compat/linux/include',
                'src/compat/posix/include'],
            "amba_registry.c"),

	"ambapp.c"
    ]
)
