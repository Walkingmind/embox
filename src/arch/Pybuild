
package('embox.arch')

module(
	'system',
	options = [
	    Integer('core_freq')
	],
)

interface('cpu')

#@Mandatory
#@DefaultImpl(embox.arch.generic.interrupt_stub)
interface('interrupt')

interface('clock')
interface('context')
interface('libarch')
interface('locore')
interface('entry')
interface('boot')
interface('lds')

#@Mandatory
#@DefaultImpl(embox.arch.generic.nommu)
interface('mmu')

#@Mandatory
#@DefaultImpl(embox.arch.generic.syscall_stub)
interface('syscall')

interface('syscall_caller')

#@Mandatory
#@DefaultImpl(embox.arch.generic.nousermode)
interface('usermode')

interface('stackframe')
