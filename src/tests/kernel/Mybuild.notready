
package('embox.test.kernel')
raise Exception('AnnotatedType not supported, do it manually')

module(
	'irq_test',

   	sources = [
		IncludePath(./src/compat/posix/include,'irq_test.c'),
	],

 	depends = [
		'embox.kernel.irq',
	],

) raise Exception('AnnotatedType not supported, do it manually')

module(
	'softirq_test',

   	sources = [
		IncludePath(./src/compat/posix/include,'softirq_test.c'),
	],

 	depends = [
		'embox.kernel.softirq',
	],

) raise Exception('AnnotatedType not supported, do it manually')

module(
	'timer_test',

   	sources = [
		IncludePath(./src/compat/posix/include,'timer_test.c'),
	],

 	depends = [
		'embox.kernel.timer.timer_api',
 		'embox.kernel.time.timer',
	],

) raise Exception('AnnotatedType not supported, do it manually')

module(
	'periodic_timer_test',

   	sources = [
		IncludePath(./src/compat/posix/include,'periodic_timer_test.c'),
	],

 	depends = [
		'embox.kernel.timer.timer_api',
 		'embox.kernel.time.timer',
	],

) raise Exception('AnnotatedType not supported, do it manually')

module(
	'oneshot_timer_test',

   	sources = [
		IncludePath(./src/compat/posix/include,'oneshot_timer_test.c'),
	],

 	depends = [
		'embox.kernel.timer.timer_api',
 		'embox.kernel.time.timer',
	],

) 
module(
	'syscall_test',

   	sources = [
		'syscall_test.c',
	],

 	depends = [
		'embox.arch.syscall_caller',
	],

) 
module(
	'usermode_test',

   	sources = [
		'usermode_test.c',
	],

 	depends = [
		'embox.arch.syscall_caller',
	],

) 