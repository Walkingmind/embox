
package('embox.mem')

#abstract module page_api { 
	#option number page_size=4096
#}

interface('page_api')

module (
    'bitmask',
    implements = ['page_api'],
    sources = ["bitmask.c"],
    #depends = ['heap_lds'],
)
